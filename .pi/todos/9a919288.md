{
  "id": "9a919288",
  "title": "Implementare M4 Custom Compaction",
  "tags": [
    "ds4",
    "pi-extension",
    "m4"
  ],
  "status": "completed",
  "created_at": "2026-08-24T14:59:07.335Z",
  "assigned_to_session": "01a033ed-9416-706b-a65d-28ba0c981763"
}

M4 Custom Compaction completato.

Implementato:
- hook session_before_compact con CompactionCoordinator dedicato
- conservazione del cut point Pi e firstKeptEntryId
- mapping fingerprint esatto source message -> canonical Pi entry ID
- supporto messagesToSummarize + split-turn prefix
- serializzazione Pi bounded per prompt, hash SHA-256 su source canonica completa + summary precedente + file provenance
- summary contract DS4 v1 con 12 sezioni obbligatorie
- prompt injection boundary e custom instructions non override
- generazione con modello attivo, fresh routing session ID, cacheRetention none, usage restituita a Pi
- validazione deterministica: sezioni uniche/ordinate/non vuote, heading extra, file path, valori esatti backticked
- invalid/empty/tool-call/output-cap/model/storage errors -> fallback Pi default
- dettagli canonici in CompactionEntry.details.ds4ContextEngine
- cumulative readFiles/modifiedFiles anche dopo precedenti compaction DS4
- SummaryRepository con source FK e lifecycle prepared/committed/failed
- migration SQLite v5; commit/failure hooks e crash/rebuild reconciliation da Pi JSONL
- trigger proattivo una volta per leaf: min(model soft limit, fixed + adaptive tail + segment target)
- /context compaction e /context compact-preview
- preserveRecentVerbatim=false rifiutato per mantenere invarianti non distruttivi
- docs COMPACTION, architecture, storage e ADR aggiornati

Verifica:
- npm run check: 19 file / 50 test passati
- unit summary contract/golden/source hash/adapter split-turn/cumulative files
- integration custom generation, validation fallback, output-cap fallback, prepared failure, proactive guard, persistence e resume
- migration/repository transaction e missing-source rollback
- npm pack --dry-run: 41 file, OK
- Pi 0.84.3 SQLite health schema v5 OK
- E2E reale RPC offline con faux provider: custom summary valid, fromHook=true, 2 source IDs, Pi CompactionEntry append, DB lifecycle committed e Pi entry ID correlato
- dati E2E e calibrazione faux rimossi dal DB globale.

M3 era stato committato prima dell'avvio M4 come 47b3c45.
