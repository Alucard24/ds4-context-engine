{
  "id": "3d11e0ae",
  "title": "Implementare M6 Historical Retrieval",
  "tags": [
    "ds4",
    "pi-extension",
    "m6"
  ],
  "status": "completed",
  "created_at": "2026-08-24T16:08:27.413Z",
  "assigned_to_session": "01a033ed-9416-706b-a65d-28ba0c981763"
}

M6 Historical Retrieval completato.

Implementato:
- TaskDescriptor deterministico da latest user request: backtick identifiers, file, qualified/camel/snake symbols, flags, error codes, quoted phrases, technologies, keywords EN/IT
- FTS5 MATCH query costruita solo da termini quoted con quote interne escaped; nessuna sintassi utente raw
- exact case-sensitive identifier/phrase search + FTS bm25 sul SessionIndexRepository esistente
- merge/dedup per canonical entry e contenuto normalizzato
- ranking: exact identifier/phrase, FTS order, same file/symbol/error, role authority, recency, token penalty
- raw message/custom_message evidence soltanto; active-context duplicates esclusi
- branch isolation hard: solo entry ID in SessionManager.getBranch; sibling candidates contati ma testo/reason non inviati
- excerpt centrato sul match, max 6000 chars, token-fit deterministico
- evidence block con source/date/role/score/reason e contenuto JSON.stringify data-only anti prompt-injection
- supplemental planner groups atomici inseriti prima del current request
- priorità planner recent 100 > retrieval 85 > summary 75
- maxRetrievedHistoryTokens + maxResults 1..100
- planner fallback elimina tutti i synthetic retrieval message e restituisce AgentMessage[] Pi originale
- exact failure fail-open; FTS failure conserva exact hit; semantic=true warning-only
- manifest kind retrieval + retrievedEventIds, source IDs e reasons senza evidence text
- synthetic provenance mapping separato: non consuma role/order source del current user
- RuntimeDiagnostics e /context retrieved con candidate/branch/dedup/planner/token/latency details
- plannerVersion managed-retrieval-v1, policyVersion 2
- docs RETRIEVAL, planner, manifest, architecture, storage, README e ADR aggiornati

Verifica:
- npm run check: 25 file / 73 test passati
- old decision recovery integration dopo Pi compaction
- branch sibling block e duplicate suppression
- exact+FTS, malformed FTS-like input escaping, FTS fallback, semantic warning
- evidence injection boundary e current-user provenance
- deterministic budget fitting/exclusion e fail-open no synthetic leakage
- config unbounded maxResults rejection
- benchmark locale 5000 indexed messages, 100 warm runs: p50 4.53 ms, p95 4.93 ms, max 6.16 ms (<50 ms target)
- npm pack --dry-run: 47 files, OK
- E2E reale Pi 0.84.3 RPC offline/faux: decision LastExportUtc compactata e assente dal summary generico, recuperata da raw entry; provider ha visto DS4 evidence marker; latest manifest contiene canonical retrievedEventId + retrieval item; current request provenance corretta; zero extension errors
- SQLite schema v6 health/integrity OK; nessuna migration necessaria per M6 (FTS M1 riusato)
- dati E2E isolati in /tmp e rimossi; DB globale senza sessioni/calibrazioni faux residue.

M5 committato prima dell'avvio M6 come 9424da8.
