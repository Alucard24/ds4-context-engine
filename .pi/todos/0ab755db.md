{
  "id": "0ab755db",
  "title": "Implementare M10 Privacy and Provider Policy",
  "tags": [
    "ds4",
    "pi-extension",
    "m10"
  ],
  "status": "completed",
  "created_at": "2026-08-24T21:25:32.851Z",
  "assigned_to_session": "01a033ed-9416-706b-a65d-28ba0c981763"
}

M10 Privacy and Remote Provider Policy completato.

Implementazione:
- classificazioni `normal < internal < sensitive < local-only`; marker span/prefix case-insensitive, escape con backslash, nested/mismatched/split marker fail-closed e divieto di downgrade;
- configurazione opt-in: provider locali exact-ID, unknown provider remoto, allow list remota default/per-provider, dynamic global+project merge, overlap/duplicate/`local-only` validation, secret redaction;
- privacy core Pi-independent con sanitizer ricorsivo cycle-safe per text/thinking/tool arguments/results/images/dynamic keys, system/tools/provider payload; protocol IDs preservati;
- secret redactor per private key, Bearer, OpenAI/Anthropic-style key, GitHub token, AWS access ID e key/token/password assignment;
- context hook sanitizza native messages prima di artifact/planner; native atomic/current groups ricevono placeholder, synthetic pin/memory/retrieval/project con blocchi vietati sono esclusi interamente;
- planner metadata classification/privacy reason; priority/budget/atomicity invariati; planner failure usa native context già sanitizzato;
- `before_provider_request` finale provider-aware su serializer Pi 0.84.3 (`system/messages/input/contents/context/tools/metadata`); il sanitizer non propaga eccezioni e restituisce `{}` su failure per forzare request failure senza leak;
- system prompt/tool definitions sanitizzati per manifest/token accounting e ricontrollati sulla forma provider effettiva;
- pin/memory supportano `--classification`, persistono classification nella mutation Pi canonica e `metadata_json`; supersession la preserva salvo override;
- historical/project evidence privacy-filtered con metadata-only exclusion; current-request provenance preservata;
- artifact classification persistita/derivata anche per legacy bytes; `context_artifact_search` ricontrolla classification e non restituisce excerpt remoto vietato;
- compaction sanitizza conversation/instructions/file list prima della generazione e validation; segment/aggregate ereditano il massimo classification marker, così provider switch locale→remoto resta sicuro;
- degraded/planner/privacy-preparation failure usa placeholder strutturali; provider sanitizer failure usa payload vuoto; ordinary subsystem failures restano fail-open;
- Context Manifest privacy metadata-only: destination/allow names/class counts/block/exclusion/redaction/provider-check; nessun classified text/payload/secret;
- structured logger redige marker local-only e credenziali anche dentro error strings; privacy logs solo provider/counters/error type;
- `/context privacy`; status/manifest/included/excluded/health e pin/memory output aggiornati;
- plannerVersion `managed-privacy-v1`, policyVersion 6; privacy disabilitata per default;
- documentazione PRIVACY, architecture, planner, manifest, storage, memory, artifacts, compaction, retrieval, project, README e ADR aggiornata;
- nessuna migration M10: schema resta v9, classification usa mutation/metadata JSON già versionato.

Verifica:
- `npm run check`: 37 file / 129 test passati;
- golden provider-policy contract passato;
- test normal/internal/sensitive/local-only, custom allow, unknown remote, local provider, nested/split/mismatched/no-downgrade/escaped markers, dynamic keys/images/payload cycle, credential/private-key redaction;
- integration native/system/tools, pin/memory/history/project/artifact/summary classifications, provider switch, compaction locale→remoto, manifest/log privacy, planner/preparation/provider-payload fail-closed, legacy artifact classification, observer/managed compatibility;
- benchmark 1.000-message provider payload: mean 1.75 ms, p99 3.03 ms, max 6.91 ms (<50 ms);
- `npm pack --dry-run`: 63 file, OK;
- E2E reale Pi 0.84.3 print/offline con provider faux che invoca `onPayload`: `PRIVACY_E2E_OK`; native e project local-only + credential assenti da context/payload; provider-hook probe assente; model ID preservato; manifest remote/context-and-provider, 3 blocchi, 1 source esclusa, 1 secret redaction, 1 final check; zero extension errors; canonical JSONL conserva raw user input; manifest metadata senza leak; SQLite integrity OK;
- E2E isolato `/tmp` rimosso; DB globale schema v9 integrity/FK OK, zero calibrazioni faux/privacy-faux residue.

M9 committato prima dell'avvio M10 come `8681482`.
