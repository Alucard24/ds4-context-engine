{
  "id": "dd3c0738",
  "title": "Implementare M11 Advanced Model Awareness",
  "tags": [
    "ds4",
    "pi-extension",
    "m11"
  ],
  "status": "completed",
  "created_at": "2026-08-25T05:11:57.513Z",
  "assigned_to_session": "01a033ed-9416-706b-a65d-28ba0c981763"
}

M11 Advanced Model Awareness completato.

Implementazione:
- risoluzione deterministica per profilo exact `provider/model`, con override `*` → `provider/*` → exact per context window, output ceiling, safety margin e budget recent/history/project;
- validazione config fail-safe per chiavi/field sconosciuti, limiti interi, bounds ratio e finestre; merge global + trusted project;
- tier adattivi 32k/128k/200k+ per recent tail, historical retrieval e project retrieval, sempre sottoposti all'hard budget;
- calibrazione isolata per provider/model sul raw estimate `chars-v1`; finestra bounded, hard ratio bounds, mediana + MAD, minimum accepted samples e ratio neutro finché non robusto;
- budget provider convertiti in unità estimator senza feedback ricorsivo; raw estimate del manifest resta invariato;
- soglie proactive compaction mantenute in provider-token units anche dopo calibrazione, convertendo fixed/recent/segment estimates prima del confronto con `ctx.getContextUsage()`;
- schema SQLite v10: usage uncached/cache-read/cache-write su manifest e calibration, estimator version, manifest correlation FK/unique index e migrazione legacy conservativa;
- correlazione one-shot Context Manifest → finalized assistant usage; error/aborted/missing/zero/duplicate/uncorrelated non creano sample; fallback volatile per sessioni ephemeral o persistence failure;
- cache read/write totals e share osservazionali per finestra exact model; nessuna pretesa di cache ownership/continuation canonica;
- `model_select` registra source, previous profile, exact-profile reuse e cold/eligible disposition; ogni context rebuild applica nuovamente privacy per la destinazione corrente;
- switch-back recupera calibration/policy exact-model senza mutare Pi JSONL, memory/pin, project, artifact o summary provenance;
- Context Manifest metadata-only con profilo, override, bounds/sample/outlier, ratio, budget nominal/adjusted, cache e switch; finalized usage aggiornato senza prompt content;
- `/context model`; status/tokens/manifest/compaction diagnostics aggiornati;
- plannerVersion `managed-model-aware-v1`, observerVersion `observer-model-aware-v1`, policyVersion 7;
- documentazione README, MODEL_AWARENESS, architecture, planner, manifest e storage aggiornata.

Verifica:
- `npm run check`: 40 file / 141 test passati;
- unit/golden: tier 32k/128k/200k, precedence override, robust MAD/outlier, insufficient samples, calibrated hard/category budgets, cache accounting e output deterministico;
- integration: persistence/migration v9→v10, one-shot usage correlation, adaptive retrieval/project/recent, calibrated proactive compaction in provider-token units, exact-model isolation e profile reuse;
- benchmark: finestra bounded 200 sample ~9.1k ops/s; risoluzione profili 32k/128k/200k ~43.8k ops/s;
- `npm pack --dry-run`: 65 file, OK;
- E2E reale Pi 0.84.3 RPC offline con due provider faux e modelli 32k/128k/200k: 7 manifest, privacy probe `ok` in ogni turno locale/remoto, override large applicato, switch-back `profileReused=true`, calibration locale robusta (`ratio=0.926702`) dopo esclusione cold-cache outlier, cache read/write persistita;
- E2E canonical JSONL conserva raw `local-only`, manifest non contiene il contenuto privato, SQLite integrity OK/FK zero, zero `extension_error`;
- E2E isolato `/tmp` rimosso; configurazione e DB globali non modificati.

M10 committato prima dell'avvio M11 come `da5f05c`.
